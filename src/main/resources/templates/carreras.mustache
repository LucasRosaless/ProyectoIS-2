<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Gestión de Carreras</title>
    
    <!-- Hoja de estilos global -->
    <link rel="stylesheet" href="/styles.css">
    
    <!-- Iconos Boxicons -->
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
</head>
<body>
    <div class="container">
        <!-- Barra de navegación con iconos visuales de Boxicons -->
        <nav class="nav">
            <a href="/dashboard"><i class='bx bx-arrow-back'></i> Dashboard</a>
            <a href="/profesores"><i class='bx bx-id-card'></i> Profesores</a>
            <a href="/alumnos"><i class='bx bx-group'></i> Alumnos</a>
            <a href="/carreras"><i class='bx bx-git-repo-forked'></i> Carreras</a>
            <a href="/planes"><i class='bx bx-book'></i> Planes</a>
            <a href="/materias"><i class='bx bx-book-bookmark'></i> Materias</a>
        </nav>

        <!-- Panel principal con estilo glassmorphism -->
        <div class="glass-panel">
            <div class="header-flex">
                <h2><i class='bx bx-git-repo-forked'></i> Lista de Carreras</h2>
                <a href="/carreras/new" class="btn btn-primary">
                    <i class='bx bx-plus'></i> Nueva Carrera
                </a>
            </div>

            <!-- Tabla de carreras con iconos en las acciones -->
            <div class="table-container">
                <table>
                    <thead>
                        <tr>
                            <th>ID Carrera</th>
                            <th>Código</th>
                            <th>Nombre</th>
                            <th>Duración (Años)</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#carreras}}
                        <tr>
                            <td>{{id_carrera}}</td>
                            <td>{{codigo}}</td>
                            <td>{{nombre}}</td>
                            <td>{{duracion_anios}}</td>
                            <td class="actions">
                                <a href="/carreras/{{id_carrera}}/edit" class="btn btn-secondary">
                                    <i class='bx bx-edit-alt'></i> Editar
                                </a>
                                <form action="/carreras/{{id_carrera}}/delete" method="POST" style="display:inline;">
                                    <button type="submit" class="btn btn-danger" onclick="return confirm('¿Seguro que desea eliminar esta carrera?');">
                                        <i class='bx bx-trash'></i> Borrar
                                    </button>
                                </form>
                            </td>
                        </tr>
                        {{/carreras}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>
