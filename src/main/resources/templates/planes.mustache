<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Gestión de Planes de Estudio</title>
    
    <!-- Hoja de estilos global -->
    <link rel="stylesheet" href="/styles.css">
    
    <!-- Iconos Boxicons -->
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
</head>
<body>
    <div class="container">
        <!-- Barra de navegación con iconos visuales de Boxicons -->
        <nav class="nav">
            <a href="/dashboard"><i class='bx bx-arrow-back'></i> Dashboard</a>
            <a href="/profesores"><i class='bx bx-id-card'></i> Profesores</a>
            <a href="/alumnos"><i class='bx bx-group'></i> Alumnos</a>
            <a href="/carreras"><i class='bx bx-git-repo-forked'></i> Carreras</a>
            <a href="/planes"><i class='bx bx-book'></i> Planes</a>
            <a href="/materias"><i class='bx bx-book-bookmark'></i> Materias</a>
        </nav>

        <!-- Panel principal con estilo glassmorphism -->
        <div class="glass-panel">
            <div class="header-flex">
                <h2><i class='bx bx-book'></i> Lista de Planes de Estudio</h2>
                <a href="/planes/new" class="btn btn-primary">
                    <i class='bx bx-plus'></i> Nuevo Plan
                </a>
            </div>

            <!-- Tabla de planes con iconos en las acciones -->
            <table>
                <thead>
                    <tr>
                        <th>ID Plan</th>
                        <th>Resolución</th>
                        <th>Año Vigencia</th>
                        <th>Estado</th>
                        <th>ID Carrera</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {{#planes}}
                    <tr>
                        <td>{{id_plan}}</td>
                        <td>{{resolucion}}</td>
                        <td>{{anio_vigencia}}</td>
                        <td>{{estado}}</td>
                        <td>{{id_carrera}}</td>
                        <td class="actions">
                            <a href="/planes/{{id_plan}}/edit" class="btn btn-secondary">
                                <i class='bx bx-edit-alt'></i> Editar
                            </a>
                            <form action="/planes/{{id_plan}}/delete" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-danger" onclick="return confirm('¿Seguro que desea eliminar este plan de estudio?');">
                                    <i class='bx bx-trash'></i> Borrar
                                </button>
                            </form>
                        </td>
                    </tr>
                    {{/planes}}
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
